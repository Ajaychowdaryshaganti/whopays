{
  "name": "whopays",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"tsx apps/api/server/index.ts\" \"cd apps/frontend && vite\"",
    "build": "cd apps/frontend && vite build",
    "lint": "oxlint",
    "preview": "cd apps/frontend && vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "oxlint": "^1.71.0",
    "tsx": "^4.19.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
